name: Experiment / Preview Build

on:
  push:
    branches:
      - develop
      - sunday/release
    tags: [ "v*" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'screenshots/**'
      - 'LICENSE'
  workflow_dispatch:
    inputs:
      channel:
        description: Build channel
        required: false
        default: Experiment
        type: choice
        options:
          - Experiment
          - Preview

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  resolve_mode:
    runs-on: ubuntu-latest
    outputs:
      channel: ${{ steps.mode.outputs.channel }}
      is_preview: ${{ steps.mode.outputs.is_preview }}
      has_gitee_token: ${{ steps.gitee_token.outputs.has_token }}
    steps:
      - id: mode
        shell: bash
        run: |
          CHANNEL="Experiment"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.channel }}" ]; then
            CHANNEL="${{ inputs.channel }}"
          elif [ "${{ github.ref_name }}" = "develop" ] && [[ "${{ github.event.head_commit.message }}" == *"[preview]"* ]]; then
            CHANNEL="Preview"
          fi
          if [ "$CHANNEL" = "Preview" ]; then
            echo "is_preview=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_preview=false" >> "$GITHUB_OUTPUT"
          fi
          echo "channel=$CHANNEL" >> "$GITHUB_OUTPUT"
      - id: gitee_token
        shell: bash
        run: |
          HAS="${{ secrets.gitee_token || secrets.GITEE_TOKEN }}"
          if [ -n "$HAS" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
          fi

  pre_job:
    needs: resolve_mode
    runs-on: ubuntu-latest
    outputs:
      should_skip: ${{ steps.skip_result.outputs.should_skip }}
    steps:
      - id: skip_bypass
        if: needs.resolve_mode.outputs.is_preview == 'true'
        shell: bash
        run: |
          echo "should_skip=false" >> "$GITHUB_OUTPUT"

      - id: skip_check
        if: needs.resolve_mode.outputs.is_preview != 'true'
        uses: fkirc/skip-duplicate-actions@v5
        with:
          skip_after_successful_duplicate: 'true'
          concurrent_skipping: 'same_content'

      - id: skip_result
        shell: bash
        run: |
          if [ "${{ needs.resolve_mode.outputs.is_preview }}" = "true" ]; then
            echo "should_skip=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_skip=${{ steps.skip_check.outputs.should_skip }}" >> "$GITHUB_OUTPUT"
          fi

  build-all:
    needs:
      - resolve_mode
      - pre_job
    if: |
      (needs.resolve_mode.outputs.is_preview == 'true' || needs.pre_job.outputs.should_skip != 'true') &&
      !contains(github.event.head_commit.message, '[skip canary]') &&
      !contains(github.event.head_commit.message, '[skip experiment]') &&
      !contains(github.event.head_commit.message, '[skip preview]')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Resolve Build Metadata
        id: build_meta
        shell: bash
        run: |
          CODENAME=$(sed -n 's/^[[:space:]]*appCodename[[:space:]]*=[[:space:]]*//p' gradle.properties | tail -n 1 | tr -d '\r')
          if [ -z "$CODENAME" ]; then
            CODENAME="Unassigned"
          fi
          VERSION_PREFIX="$(TZ=Asia/Shanghai date +'%y').$(TZ=Asia/Shanghai date +'%-m')"
          echo "codename=$CODENAME" >> $GITHUB_OUTPUT
          echo "version_prefix=$VERSION_PREFIX" >> $GITHUB_OUTPUT

      - name: Set up JDK 26
        uses: actions/setup-java@v5
        with:
          java-version: '26'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Check signing credentials
        id: signing
        shell: bash
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ -n "$SIGNING_KEY" ] && [ -n "$KEY_ALIAS" ] && \
             [ -n "$KEY_STORE_PASSWORD" ] && [ -n "$KEY_PASSWORD" ]; then
            echo "has_signing=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_signing=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Signing secrets are incomplete. Building a debug-signed APK; no releases or external publishing."
          fi

      - name: Build Package
        if: steps.signing.outputs.has_signing == 'true'
        run: ./gradlew assemblePrerelease -PversionPrefix="${{ steps.build_meta.outputs.version_prefix }}" -PversionChannel="${{ needs.resolve_mode.outputs.channel }}" -PappCodename="${{ steps.build_meta.outputs.codename }}"

      - name: Sign APKs
        if: steps.signing.outputs.has_signing == 'true'
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          printf '%s' "$SIGNING_KEY" | base64 --decode > release.keystore
          trap 'rm -f release.keystore' EXIT
          test -s release.keystore
          
          # Sign package generated from the prerelease build type.
          STD_APK=$(find app/build/outputs/apk/prerelease -name "*.apk" ! -name "*-signed.apk" | head -n 1)
          ${ANDROID_HOME}/build-tools/37.0.0/apksigner sign \
            --ks release.keystore \
            --ks-key-alias "$KEY_ALIAS" \
            --ks-pass env:KEY_STORE_PASSWORD \
            --key-pass env:KEY_PASSWORD \
            --out app/build/outputs/apk/prerelease/app-prerelease-signed.apk \
            "$STD_APK"

      - name: Generate MD5 Checksum
        if: steps.signing.outputs.has_signing == 'true'
        id: md5
        run: |
          MD5_VALUE=$(md5sum app/build/outputs/apk/prerelease/app-prerelease-signed.apk | awk '{print $1}')
          echo "md5=${MD5_VALUE}" >> "$GITHUB_OUTPUT"


      - name: Upload Signed Artifact
        if: steps.signing.outputs.has_signing == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: Capsulyric-${{ needs.resolve_mode.outputs.channel }}-Signed
          path: |
            app/build/outputs/apk/prerelease/app-prerelease-signed.apk

      - name: Build debug APK (no signing secrets)
        if: steps.signing.outputs.has_signing != 'true'
        run: ./gradlew assembleDebug -PversionPrefix="${{ steps.build_meta.outputs.version_prefix }}" -PversionChannel=Debug -PappCodename="${{ steps.build_meta.outputs.codename }}"

      - name: Verify debug APK signature
        if: steps.signing.outputs.has_signing != 'true'
        run: |
          "${ANDROID_HOME}/build-tools/37.0.0/apksigner" verify --verbose app/build/outputs/apk/debug/app-debug.apk

      - name: Upload debug APK
        if: steps.signing.outputs.has_signing != 'true'
        uses: actions/upload-artifact@v6
        with:
          name: Capsulyric-${{ needs.resolve_mode.outputs.channel }}-Debug-Signed
          if-no-files-found: error
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Generate Experiment Metadata
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]')
        id: meta
        run: |
          COUNT=$(git rev-list --count HEAD)
          SHORT_SHA=$(git rev-parse --short=7 HEAD)
          VERSION_PREFIX="${{ steps.build_meta.outputs.version_prefix }}"
          CODENAME="${{ steps.build_meta.outputs.codename }}"
          BRANCH_NAME="${GITHUB_REF_NAME}"
          BRANCH_SLUG=$(echo "$BRANCH_NAME" | sed 's#[^A-Za-z0-9._-]#-#g')
          DISPLAY_VERSION="${VERSION_PREFIX}.Experiment_C${COUNT}"
          echo "count=$COUNT" >> $GITHUB_OUTPUT
          echo "short_sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
          echo "branch_slug=$BRANCH_SLUG" >> $GITHUB_OUTPUT
          
          # Find the most recent previous shared Canary tag for changelog comparison
          PREV_TAG=$(git tag --sort=-creatordate | grep -E '^Canary\.Version_C[0-9]+$' | head -n 1)
          PREV_SHA=""
          if [ -n "$PREV_TAG" ]; then
            PREV_SHA=$(git rev-list -n 1 "$PREV_TAG" 2>/dev/null || echo "")
          fi
          
          # Base name of the release
          REL_NAME="${DISPLAY_VERSION}"
          TAG_NAME="Canary.Version_C${COUNT}"
          echo "rel_name=$REL_NAME" >> $GITHUB_OUTPUT
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT

          # Prepare Changelog Body
          echo "## **实验性版本 (Experimental Build)**" > canary_body.md
          echo "这是自动生成的 Experiment 滚动构建。可能包含不稳定的代码或破坏性变更。" >> canary_body.md
          echo "This is a rolling Experiment release generated automatically. It may contain unstable code or breaking changes." >> canary_body.md
          echo "" >> canary_body.md
          echo "## Build Info" >> canary_body.md
          echo "- **Codename:** \`${CODENAME}\`" >> canary_body.md
          echo "- **Version:** \`${DISPLAY_VERSION}\`" >> canary_body.md
          echo "- **Latest Commit:** \`${SHORT_SHA}\`" >> canary_body.md
          echo "- **Branch:** \`${BRANCH_NAME}\`" >> canary_body.md
          echo "- **MD5:** \`${{ steps.md5.outputs.md5 }}\`" >> canary_body.md
          echo "" >> canary_body.md
          echo "### Changes since last Experiment release" >> canary_body.md
          
          if [ -z "$PREV_SHA" ]; then
            echo "First build recorded." >> canary_body.md
            echo "首次构建记录" > appshare_canary_notes.txt
          elif [ "$PREV_SHA" == "$(git rev-parse HEAD)" ]; then
            echo "No new commits (Re-run or manual trigger)." >> canary_body.md
            echo "无新增提交（重新运行或手动触发）" > appshare_canary_notes.txt
          else
            # Get logs skipping merge commits, with author info and full body for GitHub/Gitee
            git log ${PREV_SHA}..HEAD --no-merges --format="- **%h**: %s (%an)%n%w(0,2,2)%b%n" >> canary_body.md

            # For AppShare: only commit change list (short hash, subject, author)
            git log ${PREV_SHA}..HEAD --no-merges --format="- **%h**: %s (%an)" | sed '/^[[:space:]]*$/d' > appshare_canary_notes.txt
            
            # Add a compare link
            echo -e "\n\n**Full Comparison:** [${PREV_SHA:0:7}...${SHORT_SHA}](https://github.com/${{ github.repository }}/compare/${PREV_SHA}...${SHORT_SHA})" >> canary_body.md
          fi
          echo "" >> canary_body.md
          echo "---" >> canary_body.md

      - name: Delete old Canary releases and tags
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CANARY_TAGS=$(git tag | grep -E '^Canary\.Version_C[0-9]+$' || true)
          if [ -n "$CANARY_TAGS" ]; then
            echo "Found existing Canary tags: $CANARY_TAGS"
            for tag in $CANARY_TAGS; do
              echo "Deleting release and tag: $tag"
              gh release delete "$tag" --yes || echo "Release $tag not found."
              git push origin --delete "$tag" || echo "Remote tag $tag not found."
            done
          else
            echo "No existing Canary tags to delete."
          fi

      - name: Delete old Canary releases on Gitee
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]') &&
          needs.resolve_mode.outputs.has_gitee_token == 'true'
        shell: bash
        env:
          GITEE_TOKEN: ${{ secrets.gitee_token || secrets.GITEE_TOKEN }}
        run: |
          echo "Deleting old Canary releases on Gitee..."
          if [ -z "$GITEE_TOKEN" ]; then
            echo "GITEE_TOKEN empty, skipping"
            exit 0
          fi
          RESP=$(curl -s "https://gitee.com/api/v5/repos/franklinsmithson/Capsulyric/releases?access_token=${GITEE_TOKEN}&per_page=100&page=1")
          if ! echo "$RESP" | jq -e 'type=="array"' >/dev/null 2>&1; then
            echo "Gitee API did not return array: $RESP"
            exit 0
          fi
          TAGS=$(echo "$RESP" | jq -r '.[] | select(.tag_name | test("^Canary\.Version_C")) | .tag_name' 2>/dev/null || true)
          if [ -z "$TAGS" ]; then
            echo "No Gitee Canary releases to delete."
            exit 0
          fi
          echo "Found Gitee Canary tags: $TAGS"
          echo "$TAGS" | while read -r tag; do
            [ -z "$tag" ] && continue
            echo "Deleting Gitee release/tag: $tag"
            curl -s -X DELETE "https://gitee.com/api/v5/repos/franklinsmithson/Capsulyric/releases/tags/$tag?access_token=${GITEE_TOKEN}" || echo "Gitee delete failed for $tag"
          done

      - name: Rolling Experiment Release
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.meta.outputs.tag_name }}
          name: ${{ steps.meta.outputs.rel_name }}
          body_path: canary_body.md
          target_commitish: ${{ steps.meta.outputs.branch_name }}
          files: |
            app/build/outputs/apk/prerelease/app-prerelease-signed.apk
          prerelease: true
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare Gitee Canary body
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]') &&
          needs.resolve_mode.outputs.has_gitee_token == 'true'
        id: gitee_canary_body
        shell: bash
        run: |
          echo "body<<GITEEDONE" >> "$GITHUB_OUTPUT"
          cat canary_body.md >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo "GITEEDONE" >> "$GITHUB_OUTPUT"

      - name: Sync Experiment Release to Gitee
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]') &&
          needs.resolve_mode.outputs.has_gitee_token == 'true'
        uses: nICEnnnnnnnLee/action-gitee-release@master
        with:
          gitee_action: create_release
          gitee_owner: franklinsmithson
          gitee_repo: Capsulyric
          gitee_token: ${{ secrets.gitee_token || secrets.GITEE_TOKEN }}
          gitee_tag_name: ${{ steps.meta.outputs.tag_name }}
          gitee_release_name: ${{ steps.meta.outputs.rel_name }}
          gitee_release_body: ${{ steps.gitee_canary_body.outputs.body }}
          gitee_target_commitish: ${{ steps.meta.outputs.branch_name }}
          gitee_files: app/build/outputs/apk/prerelease/app-prerelease-signed.apk

      - name: Publish Experiment Release to AppShare
        id: appshare_publish
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]')
        continue-on-error: true
        timeout-minutes: 15
        env:
          appKey: ${{ secrets.appKey }}
          APPSHARE_APP_KEY: ${{ secrets.appKey }}
          APPSHARE_LINK: ${{ steps.lanzou_upload.outputs.appshare_link }}
        shell: bash
        run: |
          if [ -z "$APPSHARE_LINK" ]; then
            echo "::warning::蓝奏云链接不可用，已跳过 AppShare；GitHub/Gitee 发版仍已完成。"
            exit 0
          fi
          python3 .github/scripts/publish_appshare.py \
            --apk "app/build/outputs/apk/prerelease/app-prerelease-signed.apk" \
            --name "Capsulyric-${{ steps.meta.outputs.rel_name }}.apk" \
            --app-id "AC0100AE9F074DEE9A71C8" \
            --version-code "${{ steps.meta.outputs.count }}" \
            --version-name "${{ steps.meta.outputs.rel_name }}" \
            --channel "Experiment" \
            --changelog-file "appshare_canary_notes.txt" \
            --source "Capsulyric CI (Experiment)" \
            --link "$APPSHARE_LINK"

      - name: Upload Experiment APK to LanZouCloud
        id: lanzou_upload
        if: |
          steps.signing.outputs.has_signing == 'true' &&
          github.event_name == 'push' &&
          needs.resolve_mode.outputs.is_preview != 'true' &&
          !contains(github.event.head_commit.message, '[assemble only]')
        continue-on-error: true
        timeout-minutes: 15
        env:
          LANZOU_YLOGIN: ${{ secrets.LANZOU_YLOGIN }}
          LANZOU_PHPDISK_INFO: ${{ secrets.LANZOU_PHPDISK_INFO }}
          LANZOU_PARENT_FOLDER_ID: ${{ vars.LANZOU_PARENT_FOLDER_ID }}
        shell: bash
        run: |
          python3 -m pip install --disable-pip-version-check -r .github/scripts/requirements-lanzou.txt
          PARENT_FOLDER_ID="${LANZOU_PARENT_FOLDER_ID:--1}"
          python3 .github/scripts/publish_lanzou.py \
            --apk "app/build/outputs/apk/prerelease/app-prerelease-signed.apk" \
            --channel "Experiment" \
            --version-name "${{ steps.meta.outputs.rel_name }}" \
            --parent-folder-id "$PARENT_FOLDER_ID"

      - name: Prune LanZouCloud experiment history (keep 1)
        if: ${{ steps.signing.outputs.has_signing == 'true' && steps.appshare_publish.outcome == 'success' && steps.lanzou_upload.outputs.appshare_link != '' }}
        continue-on-error: true
        timeout-minutes: 10
        env:
          LANZOU_YLOGIN: ${{ secrets.LANZOU_YLOGIN }}
          LANZOU_PHPDISK_INFO: ${{ secrets.LANZOU_PHPDISK_INFO }}
          LANZOU_PARENT_FOLDER_ID: ${{ vars.LANZOU_PARENT_FOLDER_ID }}
        shell: bash
        run: |
          python3 -m pip install --disable-pip-version-check -r .github/scripts/requirements-lanzou.txt
          PARENT_FOLDER_ID="${LANZOU_PARENT_FOLDER_ID:--1}"
          python3 .github/scripts/publish_lanzou.py \
            --channel "Experiment" \
            --version-name "${{ steps.meta.outputs.rel_name }}" \
            --keep 1 \
            --parent-folder-id "$PARENT_FOLDER_ID" \
            --prune-only
